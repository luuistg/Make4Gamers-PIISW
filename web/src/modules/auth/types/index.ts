/**
 * Tipos del módulo de autenticación.
 * Añade aquí tipos e interfaces relacionados con la autenticación.
 *
 * Ejemplos:
 *   export type { LoginCredentials, RegisterData } from './auth.types'
 */
