/**
 * Hooks del módulo de autenticación.
 * Añade aquí hooks personalizados relacionados con la autenticación.
 *
 * Ejemplos:
 *   export { useAuth } from './useAuth'
 *   export { useSession } from './useSession'
 */
