/**
 * Tipos e interfaces globales compartidos.
 * Añade aquí tipos usados en múltiples módulos.
 *
 * Ejemplos:
 *   export type { ApiResponse, PaginatedResult } from './api.types'
 *   export type { User, Session } from './user.types'
 */
