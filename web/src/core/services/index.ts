/**
 * Servicios globales (cliente API, analíticas, etc.).
 * Añade aquí servicios compartidos entre múltiples módulos.
 *
 * Ejemplos:
 *   export { apiClient } from './api.client'
 *   export { storageService } from './storage.service'
 */
